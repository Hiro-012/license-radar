[project]
name = "pep621-app"
version = "0.0.0"
dependencies = [
    "requests>=2.0",
    "flask",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "coverage",
]
docs = [
    "sphinx",
]
